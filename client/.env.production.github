# GitHub Pages production environment
# This build will connect to Replit backend for API calls

# Disable admin route (GitHub Pages only serves static files)
VITE_ENABLE_ADMIN=false

# Point to Replit backend API
# Replace with your actual Replit deployment URL after publishing
VITE_API_URL=https://jacks-lounge.replit.app/
